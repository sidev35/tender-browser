requests==2.32.3
beautifulsoup4==4.12.3

# Every source is loaded through a headless browser (see
# tender_radar/browser.py). Locally it drives the system's Microsoft Edge by default,
# so no browser download is needed; CI runs `playwright install chromium`.
playwright>=1.47,<2.0
